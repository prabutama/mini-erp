# Event Contracts

JetStream event payload schemas live here.

Initial MVP subjects are documented in `docs/nats_jetstream_event_contracts.md`.

Operations event payload drafts are in `operations.md`.
Resource event payload drafts are in `resource.md`.
