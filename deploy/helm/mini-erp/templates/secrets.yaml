apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mini-erp.fullname" . }}-secrets
  labels:
    {{- include "mini-erp.labels" . | nindent 4 }}
type: Opaque
stringData:
  POSTGRES_PASSWORD: {{ .Values.postgres.password | quote }}
  IDENTITY_JWT_SECRET: {{ .Values.jwt.secret | quote }}
